/**
 * APP / DASHBOARD
 *
 * Entradas de UI relacionadas con el panel (dashboard) de la aplicación.
 *
 * Aquí sí va:
 * - páginas del dashboard (p. ej. app/dashboard/page.tsx)
 * - subrutas específicas del panel (p. ej. app/dashboard/settings)
 * - templates/layouts exclusivos del dashboard
 *
 * Aquí NO va:
 * - lógica de negocio o servicios (mover a `modules/` o `shared/lib`)
 * - componentes genéricos reutilizables (mover a `shared/components`)
 */

export {}
